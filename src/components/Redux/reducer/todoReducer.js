const initialState = {
    isLoading: false,
    todos: [],
    error: null
};

const todosReducer = (state = initialState, action) => {
    switch (action.type) {
        case value:

            break;

        default:
            break;
    }
}